#include<iostream>
#include<string.h>
using namespace std;
void palindrome(string a, int b);
int main(){
    string str;
    cout<<"Enter any number or word:";
    cin>>str;
    int len = str.length();
    if((len%2)==0)
    palindrome(str, len);
    else 
    cout<<"❌Can't proceed because the given input has odd character !!!❌";
    return 0;
}
void palindrome(string a, int b){
    
}
